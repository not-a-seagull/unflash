language: csharp
solution: unflash.sln

jobs:
  include:
    - mono: latest

install:
 - nuget restore unflash.sln

before_script:
 - msbuild /p:Configuration=Release /p:Platform=x86 unflash.sln

script:
 - mono $TRAVIS_BUILD_DIR/unflash/bin/Release/unflash.exe
